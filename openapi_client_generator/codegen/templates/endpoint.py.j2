""" Auto-generated by openapi-client-generator
https://github.com/avanov/openapi-client-generator
"""
from typing import NamedTuple, Callable, Optional

from {{ package_name }}.common import http
from {{ package_name }}.common.types import *


__all__ = (
    'call',
    {% if path_params_type %}'Params',{% endif -%}
    {% if query_params_type %}'Query',{% endif -%}
    {% if request_type %}'Request',{% endif -%}
    {% if response_type %}'Response',{% endif -%}
)


{{ path_params_type }}

{{ query_params_type }}

{{ request_type }}

{{ response_type }}

{{ headers_type }}


METHOD = http.Method(__name__.split('.')[-1])
URL = "{{ endpoint_url }}"

{% if query_params_type %}
parse_query, dump_query = {{ query_style.value }}(Query)
{% endif %}


{% if request_type %}
parse_request, dump_request = {{ request_style.value }}(Request)
{% endif %}

{% if response_type %}
parse_response, serialize_response = {{ request_style.value }}(Response)
{% endif %}

def call(
    client: http.Client,
    {% if request_type %}request: Request,{% endif %}
    {% if path_params_type %}params: Params,{% endif %}
    {% if query_params_type %}query: Query,{% endif %}
    {% if headers_type %}headers: Headers = Headers(),{% endif %}
) -> {% if response_type %}Response{% else %}None{% endif %}:
    {% if path_params_type %}
    url = URL.format(**params._asdict())
    {% else %}
    url = URL
    {% endif %}

    resp = client.make_call(
        method=METHOD,
        url=url,
        {% if headers_type %}headers=headers._asdict(),{% endif %}
        {% if query_params_type %}query=dump_query(query),{% endif %}
        {% if request_type %}payload=dump_request(request),{% endif %}
    )
    return parse_response(resp.json())
